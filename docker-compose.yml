version: '3.8'


services:
  # nginx:
  #   depends_on:
  #     - api
  #     - client
  #   restart: always
  #   build:
  #     dockerfile: Dockerfile
  #     context: ./nginx
  #   ports:
  #     - "3050:80"

  api:
    build:
      dockerfile: Dockerfile
      context: ./todo
    volumes:
      - /app/node_modules
      - ./todo:/app
    ports:
      - "3001:3001"

  client:
    stdin_open: true
    environment:
      - CHOKIDAR_USEPOLLING=true
    build:
      dockerfile: Dockerfile
      context: ./todo_f
    volumes:
      - /app/node_modules
      - ./todo_f:/app
    ports:
      - "3000:3000"